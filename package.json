{
  "name": "note",
  "version": "0.0.1",
  "description": "application for taking quick note",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-packager . Note --platform=darwin --arch=x64 --version=1.4.11 --out=. --overwrite --icon=./images/note.icns",
    "installation": "electron-installer-dmg ./Note-darwin-x64/Note.app Note --out=. --overwrite --icon=./images/note.icns"
  },
  "productName": "Note",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shayanea/Note.git"
  },
  "keywords": [
    "note"
  ],
  "author": "Shayan Araghi",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/shayanea/Note/issues"
  },
  "homepage": "https://github.com/shayanea/Note#readme",
  "dependencies": {
    "pouchdb": "^6.1.0"
  },
  "devDependencies": {
    "electron-installer-dmg": "^0.1.2",
    "electron-packager": "^8.4.0"
  }
}
